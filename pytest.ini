[pytest]
testpaths = tests/api
markers =
    slow: mark test as slow running
    contract: schema contract validation tests
    invariant: business rule invariant tests
    ground_truth: PDF/API doc ground truth comparison tests
    regression: regression comparison tests
